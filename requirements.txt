# Core app framework
streamlit==1.54.0

# ML / model — torch installed separately in Dockerfile with CPU-only wheel
timm==1.0.27
albumentations==2.0.8
grad-cam==1.5.5

# Speech recognition
faster-whisper==1.2.1

# Audio recording (reliable cross-browser mic recording on HF Spaces)
streamlit-mic-recorder>=0.0.8

# LLM / API
google-genai==1.63.0
python-dotenv==1.2.1

# RAG / embeddings
faiss-cpu==1.13.2
sentence-transformers==5.2.2

# PDF generation — fpdf2 + uharfbuzz applies HarfBuzz GSUB shaping so Bengali
# renders correctly in Adobe Acrobat (ReportLab skipped shaping entirely)
fpdf2>=2.7.6
uharfbuzz>=0.30.0

# Hospital map
folium>=0.15.0
streamlit-folium>=0.18.0

# Image processing
Pillow==11.3.0
opencv-python-headless==4.13.0.92
numpy==2.2.6

# HTTP
requests==2.32.5

# Text-to-speech (Bengali triage audio readout)
gTTS>=2.5.0

# Testing
pytest>=7.0.0
